/**
 * Replaced at build time by build.mjs with the migration directory names from
 * artifacts/peppol-flow/prisma/migrations, in order. The server compares them
 * with `_prisma_migrations` before it starts listening.
 */
declare const __PRISMA_MIGRATIONS__: readonly string[];
